FUTURE:
 Manage some configuration (list of hosts etc...)
 Modify buzhug to return dicts (~25% boost) or lists ? (even more ?)
 
 Mettre le supprimer dans la barre de playlist
 Icone pour cacher le pattern, localhost et la barre de playlist (à droite de l'uri ?)

 Separate artist db:
  - artist view possible
  - difflib matching on insert
  much slower ?? (to test...)

 Tags edition ? add pure "tags" + ratings (user defined)
 Better tags management ? (check m4a files)

 Add tarball download to http interface

 Add playlist management to the view
  - display
  - inject (local file or directory)

  Add shoutcast/jamendo/lastfm support

 UI:
  Review alignments ?
  Contextual menu in list (with delete etc...)
  Volume Button

 Refactor:
  make actions separate files (split zshell.py)

BUGS:
  statusbar on search errors/next
  Scrollbar freezes
  Search is very limited (but fast) right now, add hybrid mode ?
   Syntax proposal:
     something
     Artist: Björk
     Artist: Björk or Artist: Noir
     title: vesp and artist: björk and length > 500
     (capitalized for case-sensitive search)

LINKS:
http://www.sacredchao.net/quodlibet/wiki/Development/Mutagen/Tutorial
http://www.pygtk.org/docs/pygtk/index.html
